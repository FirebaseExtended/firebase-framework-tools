{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "build:changed": "lerna run build --since",
    "test": "lerna run test --verbose",
    "test:changed": "learna run test --since"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^7.4.0"
  }
}